package org.example.touristguidedel3;

import org.example.touristguidedel3.model.TouristAttraction;
import org.example.touristguidedel3.repository.TouristRepository;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.List;

import static org.junit.jupiter.api.Assertions.assertFalse;

@SpringBootTest
class TouristGuideDel3ApplicationTests {

    /*

    @Autowired
    private TouristRepository touristRepository; // Inject the repository

    @Test
    void testDatabaseConnection() {
        // Fetch attractions from the database
        List<TouristAttraction> attractions = touristRepository.getAllAttractions();

        // Print the number of attractions for debugging
        System.out.println("Number of attractions: " + attractions.size());

        // Optionally, print details of attractions
        attractions.forEach(attraction ->
                System.out.println("Attraction Name: " + attraction.getName())
        );

        // Assert that the list is not empty (assuming there should be data)
        assertFalse(attractions.isEmpty(), "No attractions found in the database");
    } // Make sure this closing brace is present

} // Add this closing brace to close the class definition

*/
     */